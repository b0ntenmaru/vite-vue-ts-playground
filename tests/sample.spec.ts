import { describe, expect, it } from 'vitest';

describe('calculate', () => {
  it('1 + 1 = 2', () => {
    expect(1 + 1).toBe(2);
  });

  it('10 / 4', () => {
    expect(10 / 4).toEqual(2.5);
  });
});
